# Company logo upload location

Put your custom report logo file in this folder with the exact name:

- `company-logo.jpg`

The dashboard PDF report will try to load this file first (`../src/company-logo.jpg`).
If not found, it will fallback to `../assets/logo-jr.svg`.
